import React from 'react';
import { Container, LoadIndicator } from './styles';

const Loading = () => {
  return (
    <Container>
      <LoadIndicator />
    </Container>
  );
};

export default Loading;
